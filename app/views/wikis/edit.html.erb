<h1>Edit Wiki</h1>

<div class="row">
  <div class="col-md-4">
    <strong>Guidelines for wikis</strong>
    <ul>
      <li>Please create appropriate wikis. The creation of inappropriate wikis will result in being banned from the site.</li>
      <li>Always fact check!</li>
      <li>It's always helpful to site you resources.</li>
      <li>Here is a
        <%= link_to "quick reference", "https://en.support.wordpress.com/markdown-quick-reference", :target => "_blank" %>
        for using markdown syntax.</li>
    </ul>
    <strong>Collaborators</strong>
    <% @collabs.each do |collab| %>
    <p><%= collab.user.username %><%= link_to '  Remove', wiki_collaborator_path(@wiki, collab), method: :delete %></p>
    <% end %>

    <%= form_tag wiki_collaborators_path(@wiki) do %>
    <%= select(:collaborator, :user_id, user_collab_select_options(@new_collabs)) %>
    <%= submit_tag "Save Collaborator" %>
    <% end %>
  </div>
  <div class="col-md-8">
    <%= render partial: 'form', locals: { wiki: @wiki } %>
  </div>
</div>
